doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Add An User | Save A Life 
        script(src="add.js",defer) 
        link(rel="stylesheet", href="add.css")
    body 
        .navbar 
                ul 
                    li
                        a(href="/", target="", rel="noopener noreferrer") home
                    li
                        a(href="/add_user", target="", rel="noopener noreferrer") sing in
                    li
                        a(href="/donate", target="", rel="noopener noreferrer") Donate
                    li
                        a(href="/patient", target="", rel="noopener noreferrer") patient
                    li
                        a(href="/singout", target="", rel="noopener noreferrer") sing out
                    li
                        a(href="/login", target="", rel="noopener noreferrer") login
        form(action="/add", method="post") 
            <select onchange="print_city('state', this.selectedIndex);" id="sts" name ="stt" class="form-control" required></select>
            <select id ="state" class="form-control" name = "city"required></select>
            br
            input(type="text", name="name",placeholder="Enter your name" ,required)
            br
            input(type="number", name="contact",placeholder="Enter your contact number" ,required)
            br
            input(type="submit", value="submit")